---
- name: Make sure Vim package is installed
  package:
    name:
      - nodejs-lts-gallium
      - npm
      - vim
    state: present
  become: false

- name: Initialize/Update Vim plugins (always returns OK)
  command: vim --clean '+PlugUpdate' '+PlugClean!' '+PlugUpdate' '+qall'
  changed_when: false
