---
session_name: homelab
start_directory: ~/Projects/homelab
environment:
  KUBECONFIG: ${HOME}/Projects/homelab/output/kube_config
  GOOGLE_CREDENTIALS: ${HOME}/.secret/google_credentials.json
windows:
  - window_name: vim
    shell_command:
      - nvim
  - window_name: build
  - window_name: ssh
  - window_name: other
